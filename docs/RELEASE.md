# 文档已迁移

状态：兼容指针

当前权威入口：[发布流程](operations/release.md)

失效条件：`v0.8.4` 之后已有一个非 draft、非 prerelease 的公开 GitHub Release，且维护中 Markdown 不再含指向本文件的入站链接时删除；audit / evidence 中的历史路径记录不计入。

完整发布流程已迁移到 [docs/operations/release.md](operations/release.md)；Agent 发布禁止项见 [`.agents/rules/release.md`](../.agents/rules/release.md)。

此路径作为历史链接兼容入口保留；当前内容以目标文档为准。
