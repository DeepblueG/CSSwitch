# 文档已迁移 / Document moved

状态：兼容指针

当前权威入口：[升级与回滚](operations/upgrade-and-rollback.md)

失效条件：`v0.8.4` 之后已有一个非 draft、非 prerelease 的公开 GitHub Release，且维护中 Markdown 不再含指向本文件的入站链接时删除；audit / evidence 中的历史路径记录不计入。

请阅读 [docs/operations/upgrade-and-rollback.md](operations/upgrade-and-rollback.md)。

See [docs/operations/upgrade-and-rollback.md](operations/upgrade-and-rollback.md).

此路径作为历史链接兼容入口保留；正文只在新路径维护。
