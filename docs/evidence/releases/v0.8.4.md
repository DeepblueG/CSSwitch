# CSSwitch v0.8.4 发布证据

历史执行时间：2026-07-29（Asia/Shanghai）。远端元数据复核：2026-07-30。

本页把 release source、source gate、最终 artifact、安装身份、签名与公开分发分别
记录。历史执行结果来自当次冻结候选；2026-07-30 只重新查询了远端 tag、main 和
GitHub Release 元数据，没有重跑构建、安装或 live 场景。

## Source 与公开发布

| 项目 | 结果 |
|---|---|
| release source | `37d5cfb6600a0022d5e1bbbe9a7e181a917b12dd` |
| annotated tag object | `9c478c092e4b6f4a0bb80ca1f215bb6e60a87014` |
| peeled `v0.8.4^{}` | `37d5cfb6600a0022d5e1bbbe9a7e181a917b12dd` |
| 版本 / 架构 | `v0.8.4` / macOS Apple Silicon |
| GitHub Release | 已公开，非 draft、非 prerelease |
| 发布时间 | `2026-07-29T11:38:18Z` |
| 页面 | `https://github.com/SuperJJ007/CSSwitch/releases/tag/v0.8.4` |

2026-07-30 的只读远端复核确认 `origin/main` 与 peeled tag 仍指向 release source，
公开附件仍为下表所列 DMG。后续文档治理提交可以使 `main` 领先 tag；这不改变
v0.8.4 的 release identity。

## Source gate

在最终 clean HEAD 上执行完整 trusted `GATE-SOURCE`：

| 项目 | 结果 |
|---|---|
| run id | `712d9f75cb2d98679dfd64aed5cb1fea` |
| runner exit | `0` |
| completion seal | `PASS` |
| Rust 汇总 | 414 passed / 29 ignored / 0 failed |

该 PASS 建立 exact source 的 source/unit 证据。29 个 ignored 仍须按各自合同解释；
source gate 不替代 artifact、installed/live、真实 provider、签名或公开附件证据。

## 最终 artifact 与安装身份

| 项目 | 值 |
|---|---|
| 文件 | `CSSwitch_0.8.4_aarch64.dmg` |
| 大小 | 12,884,340 bytes |
| DMG SHA-256 | `23471daf2caa7832da3205bcc8ba97d96c057ad17539c6c7ed36aa43a3c5f2b2` |
| Desktop SHA-256 | `352763dc2008cf1b5731ed781e20066d09c678076132bced93666b547bf2dfb` |
| Gateway SHA-256 | `4448c15ef261025458486e3297188b98d96f177e627f7b2d864fe094de08757a` |

最终 App 与 DMG 从 release source 构建。自动生成的 app seal 未通过严格校验，因此
候选没有直接发布；维护者先对最终 app 执行本地 ad-hoc 重新签名，再从一次性空
staging 目录重建 DMG。最终 DMG 通过 `hdiutil verify`。

最终 `/Applications/CSSwitch.app` 版本为 0.8.4，Desktop 与 Gateway hash 如上；
安装位置只检测到一个 CSSwitch app，收尾时没有让它自动保持运行。该安装身份结论
不等于真实账号、provider、SSH server 或 Science 领域功能均通过。

同一修复线的隔离 installed 观测把旧的约 89 秒冷启动降到 7.628–9.987 秒，健康
复用为 2.195 秒；另一次观测中动态 catalog 校验仍约耗时 12.756 秒。这里保留它们
作为性能回归边界，不把先前观测自动升级为最终公开附件的全场景性能认证。

## 签名与公开附件回读

- 最终 app 的 `codesign --verify --deep --strict` 通过，但身份为 ad-hoc。
- 没有建立 Developer ID、Apple notarization、stapled ticket 或 Gatekeeper
  acceptance。
- 发布后重新下载的公开 DMG 与最终本地 DMG SHA-256 一致。
- 本地最终 DMG 与公开重下载均通过镜像校验和只读挂载根目录白名单：一个
  `CSSwitch.app` 与一个精确指向 `/Applications` 的 Applications 链接。
- 2026-07-30 GitHub 元数据仍报告同名附件、12,884,340 bytes 和相同 digest。

## v0.8.4 修复边界

- updater 固定路径只接受 exact identifier `com.anthropic.operon` 或
  `com.anthropic.operon.cli`，并要求 exact Team ID `Q6L2SF6YDW`；这仍只是
  本地身份/格式 guard，不是官方来源的密码学证明。
- 同时保留固定路径、ownership、permission、Mach-O、size、same-open copy、
  source-stability 与内容寻址 SHA-256 snapshot 的 fail-closed 检查。
- Science 大型 `conda`、`runtime`、`seed-assets`、`r-libs` 和 `sbx-bind-src`
  保持 opaque，不再被 recovery snapshot 递归复制。

## 未建立或不在本页声明中的层

- 最终公开 artifact 上所有真实第三方 provider / model 的 live 推理；
- 当前 Science 版本下所有 Project、Agent、Artifact、Skill、MCP、Reviewer、
  connector、远程计算和账号 entitlement；
- 任意真实 SSH server、云服务或付费计算；
- Developer ID、notarization、stapling 与 Gatekeeper acceptance；
- macOS Intel、Windows / WSL 或新的 Linux 正式发布。

2026-07-30 的 `B-RUNTIME-01` 是另一个更窄、受身份门禁约束的调查；它没有取得
允许的 Science executable，因而保持 `INCONCLUSIVE` / `NOT-RUN`。这不推翻本页
记录的 2026-07-29 历史 release evidence，也不能用历史结果替代该 probe。
